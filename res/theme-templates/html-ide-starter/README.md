# HTML IDE Starter

This source directory is the editable form of `html-ide-starter.theme`.

Read `docs/HTML_THEME_AUTHORING_GUIDE.md` before editing. Import the packaged
template from the HTML editor, or copy this directory into `res/themes` under a
new unique name. Never edit the template in place for a real theme.
